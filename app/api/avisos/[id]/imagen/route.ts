import{NextResponse}from"next/server";const API=(process.env.PALA_API_URL??"http://localhost:8082").trim();export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){const{id}=await params,x=await fetch(`${API}/api/avisos/${id}/imagen`,{cache:"force-cache"});if(!x.ok)return new NextResponse(null,{status:x.status});return new NextResponse(await x.arrayBuffer(),{headers:{"Content-Type":x.headers.get("content-type")??"image/jpeg","Cache-Control":"public, max-age=86400"}})}
